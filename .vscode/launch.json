{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(OSX) Launch",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceRoot}/target/debug/sudoku${pathSeparator}",
            "args": [],
            "cwd": "${workspaceRoot}",
        }
    ]
}